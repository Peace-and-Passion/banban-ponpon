export const MaxImageURLs = 30;
export const WaitForBodyTimeout: number = 1200; // ms

/** stream end annotation '{data}@' for reader.read() reads some responses on slow 3G network. */
export const streamEndAnnotation: string = "@#%";

/**for $x() query: contains(., 'text') */
export const XpathQueryTextLength: number = 20;

export const is_local: boolean = true;
